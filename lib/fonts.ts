import { Caveat, Patrick_Hand, Inter } from "next/font/google"

export const caveat = Caveat({
  subsets: ["latin"],
  weight: ["400", "700"],
})

export const sketch = Patrick_Hand({
  subsets: ["latin"],
  weight: "400",
})

export const inter = Inter({
  subsets: ["latin"],
});